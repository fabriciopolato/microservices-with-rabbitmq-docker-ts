// import dbConnect from "../config/db";

// const authService = {
//     login: async ({ name, country }) => {
//         const client = await dbConnect();
        
//         await client.query("")
//     }
// };

// export default authService;
